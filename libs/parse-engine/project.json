{
  "name": "parse-engine",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/parse-engine/convex",
  "projectType": "library",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "convex dev"
      }
    },
    "setup": {
      "executor": "nx:run-commands",
      "options": {
        "command": "convex dev --until-success"
      }
    }
  }
}
